name: Sync repository labels (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - run: echo "Disabled. Use sync-repo-metadata.yml instead; repository-metadata.json is now the canonical label and milestone source."
